import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:time_slide/app/modules/telaTime/tela_time_page.dart';

void main() {
  testWidgets('TelaTimePage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(TelaTimePage(title: 'TelaTime')));
    //  final titleFinder = find.text('TelaTime');
    //  expect(titleFinder, findsOneWidget);
  });
}
