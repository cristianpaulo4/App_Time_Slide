import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:time_slide/app/modules/timePreparacao/time_preparacao_page.dart';

void main() {
  testWidgets('TimePreparacaoPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(TimePreparacaoPage(title: 'TimePreparacao')));
    //  final titleFinder = find.text('TimePreparacao');
    //  expect(titleFinder, findsOneWidget);
  });
}
