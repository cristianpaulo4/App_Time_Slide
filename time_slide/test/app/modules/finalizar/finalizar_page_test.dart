import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:time_slide/app/modules/finalizar/finalizar_page.dart';

void main() {
  testWidgets('FinalizarPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(FinalizarPage(title: 'Finalizar')));
    //  final titleFinder = find.text('Finalizar');
    //  expect(titleFinder, findsOneWidget);
  });
}
