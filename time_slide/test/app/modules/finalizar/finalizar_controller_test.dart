import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:time_slide/app/modules/finalizar/finalizar_controller.dart';
import 'package:time_slide/app/modules/finalizar/finalizar_module.dart';

void main() {
  initModule(FinalizarModule());
  // FinalizarController finalizar;
  //
  setUp(() {
    //     finalizar = FinalizarModule.to.get<FinalizarController>();
  });

  group('FinalizarController Test', () {
    //   test("First Test", () {
    //     expect(finalizar, isInstanceOf<FinalizarController>());
    //   });

    //   test("Set Value", () {
    //     expect(finalizar.value, equals(0));
    //     finalizar.increment();
    //     expect(finalizar.value, equals(1));
    //   });
  });
}
